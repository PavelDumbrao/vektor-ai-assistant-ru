# Secrets only. Store the real values in the client HERMES_HOME/.env (mode 0600)
# or an equivalent secret manager. Never copy real values into config.yaml.
PASSIVE_SECRETARY_DATABASE_URL=<postgresql DSN for this tenant only>
PASSIVE_SECRETARY_SOURCE_REF_KEY=<at least 32 random characters>
